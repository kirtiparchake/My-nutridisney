import { useState } from 'react'
import * as Icons from 'lucide-react'
import { activities, blogs, children, games, topics } from './data/mockData'
import './App.css'

const Icon = ({ name, size = 18 }) => { const Component = Icons[name] || Icons.Circle; return <Component size={size} strokeWidth={1.8} /> }
const Button = ({ children: label, variant = 'primary', icon, ...props }) => <button className={`button button-${variant}`} {...props}>{icon && <Icon name={icon} size={16} />}{label}</button>
const Brand = ({ compact = false }) => <div className={`brand ${compact ? 'brand-compact' : ''}`}><span className="brand-mark"><Icon name="Sprout" size={20} /></span><span>Nutri<span>Disney</span></span></div>

function HomePage({ setScreen }) {
	const features = [
		['BookOpen', 'Learn about nutrition', 'Clear, age-appropriate lessons that make healthy food feel familiar.'],
		['Sparkles', 'Interactive activities', 'Short activities that turn everyday food choices into discovery.'],
		['Gamepad2', 'Fun learning games', 'Playful challenges designed to build confidence without the clutter.'],
		['TrendingUp', 'Track your progress', 'A calm, parent-friendly view of milestones, habits and achievements.'],
	]
	const steps = [
		['01', 'Parent creates an account', 'Set up your family space in a few simple steps.'],
		['02', 'Parent adds their child', 'Create a private learning profile tailored to your child.'],
		['03', 'Child uses a secure PIN', 'Children can learn and play independently in their own space.'],
	]
	return <main className="home-page">
		<nav className="home-nav"><button className="home-brand" onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}><Brand /></button><div className="home-links"><button onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}>Home</button><button onClick={() => document.getElementById('about')?.scrollIntoView({ behavior: 'smooth' })}>About</button><button onClick={() => setScreen('learn')}>Learn</button><button onClick={() => setScreen('hub')}>Knowledge hub</button></div><div className="home-actions"><button className="home-login" onClick={() => setScreen('login')}>Parent login</button><Button onClick={() => setScreen('login')} icon="ArrowRight">Get started</Button></div></nav>
		<section className="home-hero"><div className="hero-copy"><p className="eyebrow">Nutrition education for growing minds</p><h1>Helping children <em>learn, play</em> & eat better.</h1><p className="hero-subtitle">NutriDisney is an interactive nutrition learning platform that helps children build healthy eating habits through learning, activities and games.</p><div className="hero-actions"><Button onClick={() => setScreen('login')} icon="ArrowRight">Get started</Button><button className="hero-secondary" onClick={() => setScreen('learn')}>Explore nutrition <Icon name="ArrowUpRight" size={16} /></button></div><div className="hero-trust"><span className="trust-avatars"><i>JM</i><i>AM</i><i>LM</i></span><span>Made for families<br /><strong>learning together</strong></span></div></div><div className="hero-art"><div className="hero-art-glow" /><div className="hero-note note-top"><Icon name="Apple" size={17} /><span>Explore<br /><strong>9 topics</strong></span></div><div className="hero-photo" /><div className="hero-card"><span className="hero-card-icon"><Icon name="Award" size={17} /></span><span><strong>Small steps, big wins</strong><small>6 day learning streak</small></span><Icon name="ArrowUpRight" size={15} /></div><div className="hero-leaf leaf-one" /><div className="hero-leaf leaf-two" /></div></section>
		<section className="home-section feature-section" id="about"><div className="home-section-heading"><p className="eyebrow">A better way to learn about food</p><h2>Confidence starts with understanding.</h2><p>NutriDisney gives families a thoughtful, practical way to make nutrition part of everyday life.</p></div><div className="feature-grid">{features.map(([icon, title, description], index) => <article className="feature-card" key={title}><span className={`feature-icon icon-${['sage', 'coral', 'sun', 'blue'][index]}`}><Icon name={icon} size={21} /></span><span className="feature-number">0{index + 1}</span><h3>{title}</h3><p>{description}</p><button onClick={() => setScreen(index === 0 ? 'learn' : 'login')}>Explore <Icon name="ArrowUpRight" size={14} /></button></article>)}</div></section>
		<section className="home-section how-section"><div className="how-heading"><p className="eyebrow">Simple by design</p><h2>How it works</h2><p>A clear path from first sign-in to confident everyday choices.</p></div><div className="steps-grid">{steps.map(([number, title, description]) => <article className="step-card" key={number}><span className="step-number">{number}</span><div><h3>{title}</h3><p>{description}</p></div></article>)}</div></section>
		<section className="home-section audience-section"><div className="audience-card audience-parent"><div className="audience-copy"><p className="eyebrow">For parents</p><h2>Stay close to the journey, without hovering.</h2><p>Create child profiles, view learning progress, monitor completed activities and celebrate achievements as they grow.</p><div className="audience-list"><span><Icon name="Check" size={15} /> Create child profiles</span><span><Icon name="Check" size={15} /> View learning progress</span><span><Icon name="Check" size={15} /> Track achievements</span></div><Button variant="outline" onClick={() => setScreen('login')} icon="ArrowRight">Get started</Button></div><div className="audience-visual parent-visual"><div className="mini-dashboard"><span className="mini-dashboard-top" /><span className="mini-dashboard-line" /><span className="mini-dashboard-line short" /><div className="mini-dashboard-stat"><strong>72%</strong><small>learning progress</small></div></div></div></div><div className="audience-card audience-child"><div className="audience-visual child-visual"><span className="child-orbit" /><span className="child-spark spark-a">✦</span><span className="child-spark spark-b">✦</span><span className="child-circle"><Icon name="Sprout" size={34} /></span></div><div className="audience-copy"><p className="eyebrow">For children</p><h2>A space that makes learning feel like theirs.</h2><p>Learn about healthy foods, play nutrition games, complete daily challenges and earn achievements at their own pace.</p><div className="audience-list"><span><Icon name="Check" size={15} /> Learn about healthy foods</span><span><Icon name="Check" size={15} /> Complete daily challenges</span><span><Icon name="Check" size={15} /> Earn achievements</span></div></div></div></section>
		<section className="home-section hub-preview"><div className="home-section-heading split-heading"><div><p className="eyebrow">From the knowledge hub</p><h2>Practical ideas for everyday wellbeing.</h2></div><Button variant="outline" onClick={() => setScreen('hub')} icon="ArrowUpRight">Explore knowledge hub</Button></div><div className="home-blog-grid">{blogs.slice(0, 3).map((blog) => <button className="home-blog-card" key={blog.id} onClick={() => setScreen('hub')}><img src={blog.image} alt="" /><div><span className="category-label">{blog.category}</span><h3>{blog.title}</h3><span>{blog.readTime} <Icon name="ArrowUpRight" size={14} /></span></div></button>)}</div></section>
		<section className="home-cta"><div><p className="eyebrow">Begin with one small step</p><h2>Start your child's nutrition journey.</h2><p>Bring learning, play and healthier habits into the same family space.</p></div><Button variant="light" onClick={() => setScreen('login')} icon="ArrowRight">Get started</Button></section>
		<footer className="home-footer"><Brand compact /><span>Learn. Play. Eat Better.</span><span>© 2025 NutriDisney</span></footer>
	</main>
}

function AuthLayout({ children: content }) { return <main className="auth-layout"><section className="auth-visual"><div className="auth-visual-top"><Brand /><span className="eyebrow">Nutrition education, made simple</span></div><div className="auth-quote"><span className="quote-mark">“</span><h2>Small choices today can become lifelong confidence.</h2><p>Thoughtful nutrition learning for the whole family.</p></div><div className="visual-image" /><div className="visual-note"><Icon name="ShieldCheck" size={17} /> Built with families in mind</div></section><section className="auth-panel"><div className="auth-panel-inner">{content}<div className="auth-footer"><span>© 2025 NutriDisney</span><span>Privacy & safety</span></div></div></section></main> }

function Login({ setScreen }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); return <AuthLayout><div className="auth-heading"><p className="eyebrow">Parent portal</p><h1>Welcome back.</h1><p>Sign in to stay close to your child's learning journey.</p></div><form className="form-stack" onSubmit={(event) => { event.preventDefault(); if (email && password) setScreen('parent-dashboard') }}><label>Email address<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" required /></label><label>Password<div className="input-with-icon"><input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Enter your password" required /><Icon name="LockKeyhole" size={17} /></div></label><div className="form-row"><label className="check"><input type="checkbox" /> <span>Remember me</span></label><button type="button" className="text-button">Forgot password?</button></div><Button type="submit" icon="ArrowRight">Log in</Button></form><div className="auth-switch">New to NutriDisney? <button className="text-button strong" onClick={() => setScreen('register')}>Create parent account</button></div><button className="child-entry" onClick={() => setScreen('child-login')}><span className="child-entry-icon"><Icon name="KeyRound" size={18} /></span><span><strong>Child access</strong><small>Sign in with a secure PIN</small></span><Icon name="ArrowUpRight" size={17} /></button></AuthLayout> }
function Register({ setScreen }) { const [form, setForm] = useState({ name: '', email: '', password: '', confirm: '', phone: '' }); const update = (key) => (event) => setForm({ ...form, [key]: event.target.value }); return <AuthLayout><button className="back-link" onClick={() => setScreen('login')}><Icon name="ArrowLeft" size={16} /> Back to log in</button><div className="auth-heading"><p className="eyebrow">Parent portal</p><h1>Create your account.</h1><p>Make learning about food a shared family experience.</p></div><form className="form-stack" onSubmit={(event) => { event.preventDefault(); if (form.password === form.confirm && form.name && form.email) setScreen('parent-dashboard') }}><label>Your name<input value={form.name} onChange={update('name')} placeholder="Jordan Morgan" required /></label><label>Email address<input type="email" value={form.email} onChange={update('email')} placeholder="you@example.com" required /></label><div className="two-col"><label>Password<input type="password" value={form.password} onChange={update('password')} placeholder="Create a password" required /></label><label>Confirm password<input type="password" value={form.confirm} onChange={update('confirm')} placeholder="Repeat password" required /></label></div><label>Phone number <span className="optional">optional</span><input value={form.phone} onChange={update('phone')} placeholder="+44 7700 900000" /></label><Button type="submit" icon="ArrowRight">Create account</Button></form><div className="auth-switch">Already have an account? <button className="text-button strong" onClick={() => setScreen('login')}>Log in</button></div></AuthLayout> }
function ChildLogin({ setScreen }) { const [pin, setPin] = useState(''); return <AuthLayout><button className="back-link" onClick={() => setScreen('login')}><Icon name="ArrowLeft" size={16} /> Parent login</button><div className="auth-heading"><p className="eyebrow">Child access</p><h1>Ready to learn?</h1><p>Enter the six-digit PIN your parent gave you.</p></div><form className="form-stack pin-form" onSubmit={(event) => { event.preventDefault(); if (pin.length === 6) setScreen('child-home') }}><label>Child PIN<input className="pin-input" inputMode="numeric" maxLength="6" value={pin} onChange={(event) => setPin(event.target.value.replace(/\D/g, ''))} placeholder="• • • • • •" required /></label><Button type="submit" icon="ArrowRight">Enter NutriDisney</Button></form><div className="secure-note"><Icon name="ShieldCheck" size={18} /><span><strong>Your space is private</strong><small>Ask your parent if you need help finding your PIN.</small></span></div></AuthLayout> }

const parentNav = [{ id: 'parent-dashboard', label: 'Dashboard', icon: 'LayoutDashboard' }, { id: 'children', label: 'Children', icon: 'Users' }, { id: 'progress', label: 'Progress', icon: 'ChartNoAxesCombined' }, { id: 'hub', label: 'Knowledge hub', icon: 'BookOpen' }, { id: 'settings', label: 'Profile & settings', icon: 'Settings2' }]
const childNav = [{ id: 'child-home', label: 'Home', icon: 'House' }, { id: 'learn', label: 'Learn', icon: 'BookOpen' }, { id: 'games', label: 'Games', icon: 'Gamepad2' }, { id: 'challenges', label: 'Challenges', icon: 'Target' }, { id: 'child-progress', label: 'My progress', icon: 'TrendingUp' }]
function Shell({ role, screen, setScreen, children: content }) { const isChild = role === 'child'; const nav = isChild ? childNav : parentNav; return <div className={`app-shell ${isChild ? 'child-shell' : ''}`}><aside className="sidebar"><Brand compact /><div className="role-chip"><span className="status-dot" />{isChild ? "Ava's learning space" : 'Parent portal'}</div><nav>{nav.map((item) => <button key={item.id} className={screen === item.id ? 'active' : ''} onClick={() => setScreen(item.id)}><Icon name={item.icon} size={18} /><span>{item.label}</span>{item.id === 'progress' && <span className="nav-badge">6</span>}</button>)}</nav><div className="sidebar-bottom"><div className="support-box"><Icon name="MessageCircle" size={18} /><div><strong>Need a hand?</strong><small>Visit the family guide</small></div><Icon name="ArrowUpRight" size={15} /></div><button className="profile-button" onClick={() => setScreen(isChild ? 'child-progress' : 'settings')}><span className={`avatar avatar-${isChild ? 'coral' : 'sage'}`}>{isChild ? 'AM' : 'JM'}</span><span><strong>{isChild ? 'Ava Morgan' : 'Jordan Morgan'}</strong><small>{isChild ? 'Year 4 learner' : 'Parent account'}</small></span><Icon name="MoreHorizontal" size={17} /></button></div></aside><main className="main-content"><header className="topbar"><div className="mobile-brand"><Brand compact /></div><div className="breadcrumb">{isChild ? "Ava's learning space" : 'Parent portal'}<Icon name="ChevronRight" size={14} /><strong>{nav.find((item) => item.id === screen)?.label || 'Overview'}</strong></div><div className="topbar-actions"><button className="icon-button"><Icon name="Bell" size={18} /><span className="notification-dot" /></button><button className="help-button"><Icon name="CircleHelp" size={17} /> Help centre</button><button className="logout-button" onClick={() => setScreen('login')}><Icon name="LogOut" size={17} /> Log out</button></div></header><div className="page-wrap">{content}</div></main></div> }

function SectionHeader({ eyebrow, title, description, action, onAction }) { return <div className="section-header"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1>{description && <p className="section-description">{description}</p>}</div>{action && <Button onClick={onAction} variant="outline" icon="Plus">{action}</Button>}</div> }
function StatCard({ icon, label, value, note, color = 'sage' }) { return <div className="stat-card"><div className={`stat-icon icon-${color}`}><Icon name={icon} size={19} /></div><div><span>{label}</span><strong>{value}</strong><small>{note}</small></div></div> }
function ProgressBar({ value, color = 'sage' }) { return <div className="progress-track"><span className={`progress-fill fill-${color}`} style={{ width: `${value}%` }} /></div> }
function ActivityList() { return <div className="activity-list">{activities.map((activity) => <div className="activity-row" key={activity.title}><div className={`activity-icon icon-${activity.color}`}><Icon name={activity.icon} size={17} /></div><div><strong>{activity.title}</strong><small>{activity.type}</small></div><span className="activity-time">{activity.time}</span></div>)}</div> }
function ChildCard({ child, onClick }) { return <button className="child-card" onClick={onClick}><div className={`avatar avatar-${child.color}`}>{child.initials}</div><div className="child-card-main"><div className="child-card-title"><strong>{child.name}</strong><span>{child.grade} · {child.age} years</span></div><div className="child-progress-label"><span>Learning progress</span><strong>{child.progress}%</strong></div><ProgressBar value={child.progress} color={child.color === 'coral' ? 'coral' : 'sage'} /><div className="child-card-meta"><span><Icon name="Flame" size={14} /> {child.streak} day streak</span><span><Icon name="CheckCircle2" size={14} /> {child.completed} completed</span></div></div><Icon name="ChevronRight" size={19} /></button> }

function ParentDashboard({ setScreen }) { return <><SectionHeader eyebrow="Monday, 13 October 2025" title="Good morning, Jordan." description="Here's a clear view of the healthy habits your family is building." action="Add child" onAction={() => setScreen('add-child')} /><div className="dashboard-banner"><div><span className="eyebrow">Family snapshot</span><h2>Progress grows from small, consistent steps.</h2><p>Keep exploring, learning and celebrating the everyday wins.</p><Button variant="light" icon="ArrowRight" onClick={() => setScreen('progress')}>View progress</Button></div><div className="banner-orbit"><div className="orbit-ring" /><div className="banner-stat"><strong>78%</strong><span>family rhythm</span></div></div></div><div className="stats-grid"><StatCard icon="Users" label="Children learning" value="2" note="Both active this week" /><StatCard icon="CheckCircle2" label="Activities done" value="29" note="+6 this week" color="coral" /><StatCard icon="Flame" label="Best streak" value="6 days" note="Ava's current streak" color="sun" /><StatCard icon="Award" label="Badges earned" value="7" note="2 earned this month" color="blue" /></div><div className="content-grid two-thirds"><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Your children</p><h2>Learning profiles</h2></div><button className="text-button" onClick={() => setScreen('children')}>Manage children <Icon name="ArrowUpRight" size={15} /></button></div><div className="children-list">{children.map((child) => <ChildCard key={child.id} child={child} onClick={() => setScreen('progress')} />)}</div></section><section className="panel accent-panel"><div className="panel-heading"><div><p className="eyebrow">At a glance</p><h2>Weekly rhythm</h2></div><button className="icon-button"><Icon name="MoreHorizontal" size={18} /></button></div><div className="mini-chart"><div className="chart-bars">{[46, 70, 52, 88, 64, 78, 58].map((height, index) => <div className="bar-column" key={index}><span style={{ height: `${height}%` }} className={index === 3 ? 'highlight' : ''} /><small>{['M', 'T', 'W', 'T', 'F', 'S', 'S'][index]}</small></div>)}</div><div className="chart-legend"><span><i className="legend-dot" /> Learning minutes</span><strong>42 min <small>average</small></strong></div></div></section></div><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Latest updates</p><h2>Recent activity</h2></div><button className="text-button" onClick={() => setScreen('progress')}>See all activity <Icon name="ArrowRight" size={15} /></button></div><ActivityList /></section></> }
function ChildrenPage({ setScreen }) { return <><SectionHeader eyebrow="Family profiles" title="Children" description="Manage profiles and keep each learning journey personal." action="Add child" onAction={() => setScreen('add-child')} /><div className="children-grid">{children.map((child) => <ChildCard key={child.id} child={child} onClick={() => setScreen('progress')} />)}<button className="add-card" onClick={() => setScreen('add-child')}><span><Icon name="Plus" size={22} /></span><strong>Add a child</strong><small>Create a new learning profile</small></button></div><div className="info-callout"><Icon name="ShieldCheck" size={20} /><div><strong>Every child has their own private space</strong><p>Child profiles use a secure PIN, so they can explore independently while you stay in the loop.</p></div></div></> }
function AddChild({ setScreen }) { const [created, setCreated] = useState(false); const [name, setName] = useState(''); const [age, setAge] = useState(''); return <><SectionHeader eyebrow="Family profiles" title={created ? 'Profile created.' : 'Add a child'} description={created ? 'Your child is ready to begin their NutriDisney journey.' : 'Set up a private learning space for your child.'} /><div className="form-panel">{!created ? <form className="form-stack" onSubmit={(event) => { event.preventDefault(); if (name && age) setCreated(true) }}><div className="avatar-upload"><span className="avatar avatar-coral"><Icon name="UserRound" size={25} /></span><div><strong>Add a profile image</strong><small>Optional · JPG or PNG</small></div><button type="button" className="button button-outline">Upload</button></div><div className="two-col"><label>Child's name<input value={name} onChange={(event) => setName(event.target.value)} placeholder="Ava Morgan" required /></label><label>Age<input type="number" min="3" max="16" value={age} onChange={(event) => setAge(event.target.value)} placeholder="9" required /></label></div><div className="two-col"><label>Date of birth<input type="date" /></label><label>Gender<select defaultValue="prefer"><option value="prefer">Prefer not to say</option><option>Girl</option><option>Boy</option><option>Non-binary</option></select></label></div><label>Dietary preferences <span className="optional">optional</span><select defaultValue="none"><option value="none">No preferences to add</option><option>Vegetarian</option><option>Vegan</option><option>Gluten free</option></select></label><div className="form-actions"><Button type="button" variant="ghost" onClick={() => setScreen('children')}>Cancel</Button><Button type="submit" icon="ArrowRight">Create profile</Button></div></form> : <div className="success-card"><div className="success-icon"><Icon name="Check" size={25} /></div><p className="eyebrow">Private child access</p><h2>{name || 'Ava'}'s login PIN</h2><div className="pin-display">482731</div><p>This PIN allows your child to access NutriDisney. Keep it private.</p><div className="success-actions"><Button onClick={() => setScreen('children')} variant="outline">Back to children</Button><Button onClick={() => setScreen('child-home')} icon="ArrowRight">Preview child space</Button></div></div>}</div></> }

function ProgressPage({ childMode = false }) { const child = children[0]; return <><SectionHeader eyebrow={childMode ? "Ava's learning" : 'Family insights'} title={childMode ? 'My progress' : `${child.name}'s progress`} description={childMode ? 'See what you have learned and what is waiting next.' : 'A simple view of learning momentum, activities and confidence.'} /><div className="stats-grid"><StatCard icon="CheckCircle2" label="Activities completed" value={child.completed} note="+4 this week" /><StatCard icon="Gamepad2" label="Games completed" value="9" note="2 new this week" color="coral" /><StatCard icon="Flame" label="Learning streak" value={`${child.streak} days`} note="Keep it going" color="sun" /><StatCard icon="Award" label="Badges earned" value="4" note="Explorer level" color="blue" /></div><div className="content-grid two-thirds"><section className="panel progress-chart-panel"><div className="panel-heading"><div><p className="eyebrow">Last 7 days</p><h2>Learning minutes</h2></div><span className="chart-value">42 <small>min average</small></span></div><div className="large-chart"><div className="y-labels"><span>60</span><span>40</span><span>20</span><span>0</span></div><div className="chart-area"><div className="grid-lines"><i /><i /><i /><i /></div><div className="line-chart">{[36, 50, 28, 68, 52, 78, 64].map((height, index) => <div className="line-point" key={index} style={{ bottom: `${height}%` }}><b /><span>{height - 10}m</span></div>)}</div><div className="x-labels">{['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'].map((day) => <span key={day}>{day}</span>)}</div></div></div></section><section className="panel badge-panel"><div className="panel-heading"><div><p className="eyebrow">Milestones</p><h2>Badges earned</h2></div><span className="badge-count">4 / 8</span></div><div className="badges">{['First steps', 'Quiz whiz', 'Water wise', 'Food explorer'].map((badge, index) => <div className="badge-item" key={badge}><span className={`badge-symbol badge-${index}`}><Icon name={['Footprints', 'Brain', 'Droplets', 'Compass'][index]} size={19} /></span><strong>{badge}</strong><small>{['Started learning', '3 quizzes completed', '7 day challenge', '5 topics explored'][index]}</small></div>)}</div></section></div><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Completed recently</p><h2>Learning timeline</h2></div></div><ActivityList /></section></> }
function LearnPage() { const [selected, setSelected] = useState(null); return <><SectionHeader eyebrow="Nutrition library" title="Learn nutrition" description="Build a clear, friendly foundation for better everyday choices." /><div className="topic-grid">{topics.map((topic) => <button className="topic-card" key={topic.id} onClick={() => setSelected(topic)}><span className={`topic-icon icon-${topic.color}`}><Icon name={topic.icon} size={21} /></span><strong>{topic.title}</strong><p>{topic.description}</p><span className="topic-link">Explore topic <Icon name="ArrowUpRight" size={14} /></span></button>)}</div>{selected && <div className="modal-backdrop" onClick={() => setSelected(null)}><div className="modal-card" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={() => setSelected(null)}><Icon name="X" size={19} /></button><span className={`topic-icon icon-${selected.color}`}><Icon name={selected.icon} size={23} /></span><p className="eyebrow">Nutrition basics</p><h2>{selected.title}</h2><p className="modal-lead">{selected.description}</p><div className="lesson-grid"><div><span>Why it matters</span><strong>It helps your body learn, grow and have energy for the things you enjoy.</strong></div><div><span>Food examples</span><strong>{selected.examples}</strong></div></div><div className="fun-fact"><Icon name="Lightbulb" size={19} /><p><strong>Fun fact</strong>{selected.fact}</p></div><Button icon="ArrowRight" onClick={() => setSelected(null)}>Mark as explored</Button></div></div>}</> }
function GamesPage() { const [active, setActive] = useState(null); const [score, setScore] = useState(0); return <><SectionHeader eyebrow="Play & practise" title="Games & activities" description="Short, thoughtful challenges that turn nutrition knowledge into confidence." /><div className="game-grid">{games.map((game) => <div className="game-card" key={game.id}><div className="game-card-top"><span className={`topic-icon icon-${game.color}`}><Icon name={game.icon} size={21} /></span><span className="difficulty">{game.difficulty}</span></div><h2>{game.title}</h2><p>{game.description}</p><div className="game-meta"><span>{game.meta}</span><span>{game.progress}% complete</span></div><ProgressBar value={game.progress} color={game.color === 'sun' ? 'coral' : game.color} /><Button variant="outline" onClick={() => { setActive(game); setScore(0) }}>{game.progress ? 'Continue' : 'Start activity'} <Icon name="ArrowRight" size={15} /></Button></div>)}</div>{active && <div className="modal-backdrop" onClick={() => setActive(null)}><div className="modal-card quiz-modal" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={() => setActive(null)}><Icon name="X" size={19} /></button><span className="topic-icon icon-sage"><Icon name="Brain" size={22} /></span><p className="eyebrow">Quick round · Question 1 of 3</p><h2>{active.title}</h2><p className="modal-lead">Which choice gives your body a good mix of energy and nutrients?</p><div className="quiz-options">{['A colourful plate with different food groups', 'Only one favourite food every day', 'Skipping breakfast to save time'].map((option, index) => <button key={option} onClick={() => setScore(index === 0 ? 1 : 0)} className={score && index === 0 ? 'correct' : ''}><span>{String.fromCharCode(65 + index)}</span>{option}</button>)}</div>{score > 0 && <div className="quiz-feedback"><Icon name="CheckCircle2" size={18} /> Nice work. Variety helps make a balanced plate.</div>}<Button onClick={() => setActive(null)} icon="ArrowRight">Finish round</Button></div></div>}</> }
function HubPage({ setScreen }) { return <><SectionHeader eyebrow="Knowledge hub" title="Ideas for everyday wellbeing" description="Clear, practical reading for families building healthy habits together." /><div className="hub-feature"><div className="hub-feature-image" /><div><p className="eyebrow">Featured guide</p><h2>Making food learning part of family life</h2><p>A calm, practical starting point for talking about food, trying new things and building confidence around the table.</p><Button icon="ArrowRight" onClick={() => setScreen('blog-detail')}>Read the guide</Button></div></div><div className="hub-toolbar"><h2>Latest from the hub</h2><div className="filter-pills"><button className="active">All articles</button><button>Parent guides</button><button>Healthy habits</button><button>Child nutrition</button></div></div><div className="blog-grid">{blogs.map((blog) => <button className="blog-card" key={blog.id} onClick={() => setScreen('blog-detail')}><img src={blog.image} alt="" /><div className="blog-card-body"><span className="category-label">{blog.category}</span><h2>{blog.title}</h2><p>{blog.description}</p><span className="read-more">{blog.readTime} <Icon name="ArrowUpRight" size={14} /></span></div></button>)}</div></> }
function BlogDetail({ setScreen }) { const blog = blogs[0]; return <><button className="back-link page-back" onClick={() => setScreen('hub')}><Icon name="ArrowLeft" size={16} /> Back to knowledge hub</button><article className="article"><span className="category-label">{blog.category}</span><h1>{blog.title}</h1><p className="article-intro">{blog.description}</p><div className="article-meta"><span>By the NutriDisney learning team</span><span>{blog.readTime}</span></div><img className="article-image" src={blog.image} alt="Fresh food arranged on a table" /><div className="article-copy"><p>Food learning works best when it feels connected to real life. A shared breakfast, a trip to the market or a new ingredient at dinner can all become small opportunities to notice, ask questions and build confidence.</p><h2>Start with curiosity, not rules</h2><p>Children learn through repetition and example. Keeping conversations open can make it easier to explore a variety of foods without turning meals into a test. You can talk about colour, texture, smell and the different ways food helps our bodies.</p><div className="article-callout"><Icon name="MessageCircle" size={20} /><strong>Try this together: choose one food on your plate and describe three things you notice about it.</strong></div><h2>Let variety grow over time</h2><p>A balanced pattern is built across days and weeks. Small, consistent choices give children room to develop their own preferences while learning how different foods fit together.</p></div></article></> }
function SettingsPage() { return <><SectionHeader eyebrow="Your account" title="Profile & settings" description="Manage your family details and account preferences." /><div className="settings-layout"><aside className="settings-nav"><button className="active"><Icon name="UserRound" size={17} /> Profile</button><button><Icon name="Users" size={17} /> Child management</button><button><Icon name="Bell" size={17} /> Notifications</button><button><Icon name="ShieldCheck" size={17} /> Privacy & safety</button></aside><section className="form-panel settings-form"><div className="avatar-upload"><span className="avatar avatar-sage">JM</span><div><strong>Jordan Morgan</strong><small>Parent account · jordan@example.com</small></div><button className="button button-outline">Change photo</button></div><div className="form-divider" /><div className="two-col"><label>First name<input defaultValue="Jordan" /></label><label>Last name<input defaultValue="Morgan" /></label></div><label>Email address<input defaultValue="jordan@example.com" /></label><label>Phone number<input defaultValue="+44 7700 900000" /></label><div className="form-actions"><Button variant="outline">Cancel</Button><Button icon="Check">Save changes</Button></div></section></div></> }
function ChildHome({ setScreen }) { return <><div className="child-welcome"><div><p className="eyebrow">Tuesday, 14 October</p><h1>Ready for a good one, Ava?</h1><p>Pick up where you left off or try something new today.</p></div><div className="child-mascot"><Icon name="Sparkles" size={28} /><span>6 day<br />streak</span></div></div><div className="child-feature-grid"><button className="learning-hero" onClick={() => setScreen('learn')}><div><span className="eyebrow">Today's learning</span><h2>What makes a balanced plate?</h2><p>Explore the four parts that help make a meal feel complete.</p><span className="button button-light">Continue learning <Icon name="ArrowRight" size={15} /></span></div><div className="plate-art"><span /><span /><span /><span /></div></button><section className="child-side-card"><div className="panel-heading"><div><p className="eyebrow">Daily challenge</p><h2>Water wise</h2></div><span className="challenge-icon"><Icon name="GlassWater" size={18} /></span></div><p>Can you remember to take 6 water breaks today?</p><ProgressBar value={45} color="blue" /><div className="challenge-footer"><strong>3 of 6 breaks</strong><button className="text-button" onClick={() => setScreen('challenges')}>View <Icon name="ArrowRight" size={14} /></button></div></section></div><div className="child-section-heading"><div><p className="eyebrow">Choose your path</p><h2>Keep exploring</h2></div></div><div className="child-quick-grid"><button onClick={() => setScreen('games')}><span className="topic-icon icon-coral"><Icon name="Gamepad2" size={22} /></span><strong>Play a game</strong><small>Learn by doing</small><Icon name="ArrowUpRight" size={15} /></button><button onClick={() => setScreen('learn')}><span className="topic-icon icon-sage"><Icon name="BookOpen" size={22} /></span><strong>Learn something</strong><small>Explore 9 topics</small><Icon name="ArrowUpRight" size={15} /></button><button onClick={() => setScreen('child-progress')}><span className="topic-icon icon-sun"><Icon name="Award" size={22} /></span><strong>My achievements</strong><small>4 badges earned</small><Icon name="ArrowUpRight" size={15} /></button></div></> }
function App() { const [screen, setScreen] = useState('home'); const role = screen.startsWith('child') || ['learn', 'games', 'challenges', 'child-progress'].includes(screen) ? 'child' : 'parent'; let page = null; if (screen === 'home') return <HomePage setScreen={setScreen} />; if (screen === 'login') return <Login setScreen={setScreen} />; if (screen === 'register') return <Register setScreen={setScreen} />; if (screen === 'child-login') return <ChildLogin setScreen={setScreen} />; if (screen === 'add-child') page = <AddChild setScreen={setScreen} />; else if (screen === 'parent-dashboard') page = <ParentDashboard setScreen={setScreen} />; else if (screen === 'children') page = <ChildrenPage setScreen={setScreen} />; else if (screen === 'progress') page = <ProgressPage />; else if (screen === 'hub') page = <HubPage setScreen={setScreen} />; else if (screen === 'blog-detail') page = <BlogDetail setScreen={setScreen} />; else if (screen === 'settings') page = <SettingsPage />; else if (screen === 'child-home') page = <ChildHome setScreen={setScreen} />; else if (screen === 'learn') page = <LearnPage />; else if (screen === 'games') page = <GamesPage />; else if (screen === 'child-progress' || screen === 'challenges') page = <ProgressPage childMode />; return <Shell role={role} screen={screen} setScreen={setScreen}>{page}</Shell> }
export default App
